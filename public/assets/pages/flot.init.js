/*
 Template Name: Veltrix - Responsive Bootstrap 4 Admin Dashboard
 Author: Themesbrand
 File: Flot chart Init
 */


!function ($) {
  "use strict";

  /*

    //initializing various charts and components
    FlotChart.prototype.init = function () {
      //Donut pie graph data
      var donutlabels = ["Wins", "Losses"];
      var donutdatas = [42,47];
      var donutcolors = ['#02a499', '#EB4334' ];
      this.createDonutGraph("#donut-chart #donut-chart-container", donutlabels, donutdatas, donutcolors);
    },

    //init flotchart
    $.FlotChart = new FlotChart, $.FlotChart.Constructor = FlotChart*/

}(window.jQuery),

  //initializing flotchart
  function ($) {
    "use strict";
    //$.FlotChart.init()
  }(window.jQuery);

  function initWithParams(donutlabels, donutdatas, donutcolors) {
      
  }


